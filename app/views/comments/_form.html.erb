<%= simple_form_for([@post, @post.comments.build]) do |f| %>
  <%= f.input :content, input_html: { class: 'form-control' } %><br>
  <%= f.button :submit, class: "button", value: "Commenter", class: "btn btn-primary" %>
<% end %>
